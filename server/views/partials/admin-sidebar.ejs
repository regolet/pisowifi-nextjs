<!-- Sidebar Navigation -->
<div class="w-64 bg-white shadow-lg flex flex-col h-screen">
    <div class="p-6 border-b">
        <div class="flex items-center">
            <div class="text-2xl mr-3">📊</div>
            <h1 class="text-xl font-bold text-gray-800">PISOWifi</h1>
        </div>
    </div>
    
    <nav class="flex-1 p-4">
        <div class="space-y-2">
            <a href="/admin" class="sidebar-item flex items-center px-4 py-3 text-gray-700 rounded-lg <%= currentPage === 'dashboard' ? 'bg-blue-50 border-r-3 border-blue-500' : 'hover:bg-gray-50' %>">
                <span class="mr-3">🏠</span>
                <span>Dashboard</span>
            </a>
            <a href="/admin/clients" class="sidebar-item flex items-center px-4 py-3 text-gray-700 rounded-lg <%= currentPage === 'clients' ? 'bg-blue-50 border-r-3 border-blue-500' : 'hover:bg-gray-50' %>">
                <span class="mr-3">👥</span>
                <span>Clients</span>
            </a>
            <a href="/admin/rates" class="sidebar-item flex items-center px-4 py-3 text-gray-700 rounded-lg <%= currentPage === 'rates' ? 'bg-blue-50 border-r-3 border-blue-500' : 'hover:bg-gray-50' %>">
                <span class="mr-3">🪙</span>
                <span>Coin Rates</span>
            </a>
            <a href="/admin/network" class="sidebar-item flex items-center px-4 py-3 text-gray-700 rounded-lg <%= currentPage === 'network' ? 'bg-blue-50 border-r-3 border-blue-500' : 'hover:bg-gray-50' %>">
                <span class="mr-3">🌐</span>
                <span>Network</span>
            </a>
            <a href="/admin/gpio" class="sidebar-item flex items-center px-4 py-3 text-gray-700 rounded-lg <%= currentPage === 'gpio' ? 'bg-blue-50 border-r-3 border-blue-500' : 'hover:bg-gray-50' %>">
                <span class="mr-3">⚡</span>
                <span>GPIO</span>
            </a>
            <a href="/admin/reports" class="sidebar-item flex items-center px-4 py-3 text-gray-700 rounded-lg <%= currentPage === 'reports' ? 'bg-blue-50 border-r-3 border-blue-500' : 'hover:bg-gray-50' %>">
                <span class="mr-3">📊</span>
                <span>Reports</span>
            </a>
        </div>
    </nav>
    
    <div class="p-4 border-t">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-xs text-gray-500">Welcome back</p>
                <p class="text-sm font-medium text-gray-800"><%= typeof user !== 'undefined' ? user.username : 'Admin' %></p>
            </div>
            <form action="/admin/logout" method="POST">
                <button type="submit" class="text-red-600 hover:text-red-700 p-2 rounded-lg hover:bg-red-50" title="Logout">
                    <span class="text-lg">🚪</span>
                </button>
            </form>
        </div>
    </div>
</div>